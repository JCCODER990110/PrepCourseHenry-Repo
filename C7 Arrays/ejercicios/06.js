function invertirArray(array) {
  // Invierte el arreglo array recibido por argumento.
  // Tu código:


  /*
  var n = array.length;
  var invertido = [];

  while (n >= 0){
    invertido.push(array[n - 1]);
    n = n - 1;
  }

  return invertido;
  */

  return array.reverse();

}

module.exports = invertirArray;
